// @generated by idiolect-codegen from orchestrator-spec/queries.json. do not edit.

//! Generated axum handlers for each catalog query. One handler per spec entry; a register_routes helper wires them into a router.

#![allow(missing_docs, clippy::doc_markdown)]
#![allow(unused_imports)]
use crate::generated::queries as q;
use crate::http::{ApiError, AppState, EnvelopedEntry, Page, Paged};
use axum::Router;
use axum::extract::{Query, State};
use axum::routing::get;
use serde::Deserialize;

///Every cataloged bounty whose status is `open`, `claimed`, or unset.
async fn handler_open_bounties(
    State(s): State<AppState>,
    Query(page): Query<Page>,
) -> Result<axum::Json<Paged<EnvelopedEntry<idiolect_records::Bounty>>>, ApiError> {
    let catalog = s.catalog.lock()?;
    let items: Vec<_> = q::open_bounties(&catalog)
        .into_iter()
        .map(EnvelopedEntry::from)
        .collect();
    Ok(axum::Json(Paged::from_collected(items, &page)?))
}

#[derive(Debug, Deserialize)]
struct BountiesForWantLensParams {
    #[serde(rename = "source_uri")]
    raw_source: String,
    #[serde(rename = "target_uri")]
    raw_target: String,
    #[serde(flatten)]
    page: Page,
}

///Open bounties whose `wants` is a WantLens for the given schema pair.
async fn handler_bounties_for_want_lens(
    State(s): State<AppState>,
    Query(p): Query<BountiesForWantLensParams>,
) -> Result<axum::Json<Paged<EnvelopedEntry<idiolect_records::Bounty>>>, ApiError> {
    let source: idiolect_records::generated::defs::SchemaRef =
        crate::predicates::schema_ref_from_uri(p.raw_source.clone());
    let target: idiolect_records::generated::defs::SchemaRef =
        crate::predicates::schema_ref_from_uri(p.raw_target.clone());
    let catalog = s.catalog.lock()?;
    let items: Vec<_> = q::bounties_for_want_lens(&catalog, &source, &target)
        .into_iter()
        .map(EnvelopedEntry::from)
        .collect();
    Ok(axum::Json(Paged::from_collected(items, &p.page)?))
}

#[derive(Debug, Deserialize)]
struct BountiesByRequesterParams {
    #[serde(rename = "requester_did")]
    raw_requester_did: String,
    #[serde(flatten)]
    page: Page,
}

///Every bounty authored by the given requester DID.
async fn handler_bounties_by_requester(
    State(s): State<AppState>,
    Query(p): Query<BountiesByRequesterParams>,
) -> Result<axum::Json<Paged<EnvelopedEntry<idiolect_records::Bounty>>>, ApiError> {
    let requester_did: String = p.raw_requester_did.clone();
    let catalog = s.catalog.lock()?;
    let items: Vec<_> = q::bounties_by_requester(&catalog, &requester_did)
        .into_iter()
        .map(EnvelopedEntry::from)
        .collect();
    Ok(axum::Json(Paged::from_collected(items, &p.page)?))
}

#[derive(Debug, Deserialize)]
struct AdaptersForFrameworkParams {
    #[serde(rename = "framework")]
    raw_framework: String,
    #[serde(flatten)]
    page: Page,
}

///Every adapter declared for the given framework (case-insensitive).
async fn handler_adapters_for_framework(
    State(s): State<AppState>,
    Query(p): Query<AdaptersForFrameworkParams>,
) -> Result<axum::Json<Paged<EnvelopedEntry<idiolect_records::Adapter>>>, ApiError> {
    let framework: String = p.raw_framework.clone();
    let catalog = s.catalog.lock()?;
    let items: Vec<_> = q::adapters_for_framework(&catalog, &framework)
        .into_iter()
        .map(EnvelopedEntry::from)
        .collect();
    Ok(axum::Json(Paged::from_collected(items, &p.page)?))
}

#[derive(Debug, Deserialize)]
struct AdaptersByInvocationProtocolParams {
    #[serde(rename = "kind")]
    raw_kind: String,
    #[serde(flatten)]
    page: Page,
}

///Every adapter whose invocation-protocol kind matches (`subprocess`, `http`, `wasm`).
async fn handler_adapters_by_invocation_protocol(
    State(s): State<AppState>,
    Query(p): Query<AdaptersByInvocationProtocolParams>,
) -> Result<axum::Json<Paged<EnvelopedEntry<idiolect_records::Adapter>>>, ApiError> {
    let kind: idiolect_records::generated::adapter::AdapterInvocationProtocolKind =
        match crate::predicates::parse_adapter_invocation_protocol_kind(&p.raw_kind) {
            Ok(v) => v,
            Err(e) => return Err(ApiError::invalid_request(e)),
        };
    let catalog = s.catalog.lock()?;
    let items: Vec<_> = q::adapters_by_invocation_protocol(&catalog, &kind)
        .into_iter()
        .map(EnvelopedEntry::from)
        .collect();
    Ok(axum::Json(Paged::from_collected(items, &p.page)?))
}

///Every recommendation whose lens_path is non-empty.
async fn handler_recommendations_starting_from(
    State(s): State<AppState>,
    Query(page): Query<Page>,
) -> Result<axum::Json<Paged<EnvelopedEntry<idiolect_records::Recommendation>>>, ApiError> {
    let catalog = s.catalog.lock()?;
    let items: Vec<_> = q::recommendations_starting_from(&catalog)
        .into_iter()
        .map(EnvelopedEntry::from)
        .collect();
    Ok(axum::Json(Paged::from_collected(items, &page)?))
}

#[derive(Debug, Deserialize)]
struct VerificationsForLensParams {
    #[serde(rename = "lens_uri")]
    raw_lens: String,
    #[serde(flatten)]
    page: Page,
}

///Every verification in the catalog that targets the given lens.
async fn handler_verifications_for_lens(
    State(s): State<AppState>,
    Query(p): Query<VerificationsForLensParams>,
) -> Result<axum::Json<Paged<EnvelopedEntry<idiolect_records::Verification>>>, ApiError> {
    let lens: idiolect_records::generated::defs::LensRef =
        crate::predicates::lens_ref_from_uri(p.raw_lens.clone());
    let catalog = s.catalog.lock()?;
    let items: Vec<_> = q::verifications_for_lens(&catalog, &lens)
        .into_iter()
        .map(EnvelopedEntry::from)
        .collect();
    Ok(axum::Json(Paged::from_collected(items, &p.page)?))
}

#[derive(Debug, Deserialize)]
struct VerificationsByKindParams {
    #[serde(rename = "kind")]
    raw_kind: String,
    #[serde(flatten)]
    page: Page,
}

///Every verification record whose `kind` matches.
async fn handler_verifications_by_kind(
    State(s): State<AppState>,
    Query(p): Query<VerificationsByKindParams>,
) -> Result<axum::Json<Paged<EnvelopedEntry<idiolect_records::Verification>>>, ApiError> {
    let kind: idiolect_records::generated::verification::VerificationKind =
        match crate::predicates::parse_verification_kind(&p.raw_kind) {
            Ok(v) => v,
            Err(e) => return Err(ApiError::invalid_request(e)),
        };
    let catalog = s.catalog.lock()?;
    let items: Vec<_> = q::verifications_by_kind(&catalog, &kind)
        .into_iter()
        .map(EnvelopedEntry::from)
        .collect();
    Ok(axum::Json(Paged::from_collected(items, &p.page)?))
}

#[derive(Debug, Deserialize)]
struct CommunitiesForMemberParams {
    #[serde(rename = "member_did")]
    raw_member_did: String,
    #[serde(flatten)]
    page: Page,
}

///Every community whose `members` list includes the given DID.
async fn handler_communities_for_member(
    State(s): State<AppState>,
    Query(p): Query<CommunitiesForMemberParams>,
) -> Result<axum::Json<Paged<EnvelopedEntry<idiolect_records::Community>>>, ApiError> {
    let member_did: String = p.raw_member_did.clone();
    let catalog = s.catalog.lock()?;
    let items: Vec<_> = q::communities_for_member(&catalog, &member_did)
        .into_iter()
        .map(EnvelopedEntry::from)
        .collect();
    Ok(axum::Json(Paged::from_collected(items, &p.page)?))
}

#[derive(Debug, Deserialize)]
struct CommunitiesByNameParams {
    #[serde(rename = "name")]
    raw_name: String,
    #[serde(flatten)]
    page: Page,
}

///Every community whose name matches (case-insensitive).
async fn handler_communities_by_name(
    State(s): State<AppState>,
    Query(p): Query<CommunitiesByNameParams>,
) -> Result<axum::Json<Paged<EnvelopedEntry<idiolect_records::Community>>>, ApiError> {
    let name: String = p.raw_name.clone();
    let catalog = s.catalog.lock()?;
    let items: Vec<_> = q::communities_by_name(&catalog, &name)
        .into_iter()
        .map(EnvelopedEntry::from)
        .collect();
    Ok(axum::Json(Paged::from_collected(items, &p.page)?))
}

#[derive(Debug, Deserialize)]
struct DialectsForCommunityParams {
    #[serde(rename = "community_uri")]
    raw_community_uri: String,
    #[serde(flatten)]
    page: Page,
}

///Every dialect whose `owning_community` at-uri matches.
async fn handler_dialects_for_community(
    State(s): State<AppState>,
    Query(p): Query<DialectsForCommunityParams>,
) -> Result<axum::Json<Paged<EnvelopedEntry<idiolect_records::Dialect>>>, ApiError> {
    let community_uri: String = p.raw_community_uri.clone();
    let catalog = s.catalog.lock()?;
    let items: Vec<_> = q::dialects_for_community(&catalog, &community_uri)
        .into_iter()
        .map(EnvelopedEntry::from)
        .collect();
    Ok(axum::Json(Paged::from_collected(items, &p.page)?))
}

///Adapters that cite a verification record via `verification.uri`. Reference case for the expression predicate form.
async fn handler_adapters_with_verification(
    State(s): State<AppState>,
    Query(page): Query<Page>,
) -> Result<axum::Json<Paged<EnvelopedEntry<idiolect_records::Adapter>>>, ApiError> {
    let catalog = s.catalog.lock()?;
    let items: Vec<_> = q::adapters_with_verification(&catalog)
        .into_iter()
        .map(EnvelopedEntry::from)
        .collect();
    Ok(axum::Json(Paged::from_collected(items, &page)?))
}

#[derive(Debug, Deserialize)]
struct BeliefsAboutRecordParams {
    #[serde(rename = "subject_uri")]
    raw_subject_uri: String,
    #[serde(flatten)]
    page: Page,
}

///Every belief record whose subject's at-uri matches.
async fn handler_beliefs_about_record(
    State(s): State<AppState>,
    Query(p): Query<BeliefsAboutRecordParams>,
) -> Result<axum::Json<Paged<EnvelopedEntry<idiolect_records::Belief>>>, ApiError> {
    let subject_uri: String = p.raw_subject_uri.clone();
    let catalog = s.catalog.lock()?;
    let items: Vec<_> = q::beliefs_about_record(&catalog, &subject_uri)
        .into_iter()
        .map(EnvelopedEntry::from)
        .collect();
    Ok(axum::Json(Paged::from_collected(items, &p.page)?))
}

#[derive(Debug, Deserialize)]
struct BeliefsByHolderParams {
    #[serde(rename = "holder_did")]
    raw_holder_did: String,
    #[serde(flatten)]
    page: Page,
}

///Every belief record whose explicit `holder` matches the given DID. Records with an implicit holder (repo owner) are not returned here — callers needing the repo-owner fallback filter over `Entry<Belief>` directly.
async fn handler_beliefs_by_holder(
    State(s): State<AppState>,
    Query(p): Query<BeliefsByHolderParams>,
) -> Result<axum::Json<Paged<EnvelopedEntry<idiolect_records::Belief>>>, ApiError> {
    let holder_did: String = p.raw_holder_did.clone();
    let catalog = s.catalog.lock()?;
    let items: Vec<_> = q::beliefs_by_holder(&catalog, &holder_did)
        .into_iter()
        .map(EnvelopedEntry::from)
        .collect();
    Ok(axum::Json(Paged::from_collected(items, &p.page)?))
}

#[derive(Debug, Deserialize)]
struct VocabulariesWithWorldParams {
    #[serde(rename = "world")]
    raw_world: String,
    #[serde(flatten)]
    page: Page,
}

///Every vocabulary record whose declared `world` matches (`closed-with-default`, `open`, or `hierarchy-closed`).
async fn handler_vocabularies_with_world(
    State(s): State<AppState>,
    Query(p): Query<VocabulariesWithWorldParams>,
) -> Result<axum::Json<Paged<EnvelopedEntry<idiolect_records::Vocab>>>, ApiError> {
    let world: String = match crate::predicates::parse_vocab_world(&p.raw_world) {
        Ok(v) => v,
        Err(e) => return Err(ApiError::invalid_request(e)),
    };
    let catalog = s.catalog.lock()?;
    let items: Vec<_> = q::vocabularies_with_world(&catalog, &world)
        .into_iter()
        .map(EnvelopedEntry::from)
        .collect();
    Ok(axum::Json(Paged::from_collected(items, &p.page)?))
}

#[derive(Debug, Deserialize)]
struct VocabulariesByNameParams {
    #[serde(rename = "name")]
    raw_name: String,
    #[serde(flatten)]
    page: Page,
}

///Every vocabulary record whose `name` field matches exactly.
async fn handler_vocabularies_by_name(
    State(s): State<AppState>,
    Query(p): Query<VocabulariesByNameParams>,
) -> Result<axum::Json<Paged<EnvelopedEntry<idiolect_records::Vocab>>>, ApiError> {
    let name: String = p.raw_name.clone();
    let catalog = s.catalog.lock()?;
    let items: Vec<_> = q::vocabularies_by_name(&catalog, &name)
        .into_iter()
        .map(EnvelopedEntry::from)
        .collect();
    Ok(axum::Json(Paged::from_collected(items, &p.page)?))
}

/// Register every generated route onto the caller-supplied
/// router. Each query is mounted at both its REST path and
/// its `/xrpc/<nsid>` path.
#[must_use]
pub fn register_routes(router: Router<AppState>) -> Router<AppState> {
    router
        .route("/v1/bounties/open", get(handler_open_bounties))
        .route(
            "/xrpc/dev.idiolect.query.openBounties",
            get(handler_open_bounties),
        )
        .route(
            "/v1/bounties/want-lens",
            get(handler_bounties_for_want_lens),
        )
        .route(
            "/xrpc/dev.idiolect.query.bountiesForWantLens",
            get(handler_bounties_for_want_lens),
        )
        .route(
            "/v1/bounties/by-requester",
            get(handler_bounties_by_requester),
        )
        .route(
            "/xrpc/dev.idiolect.query.bountiesByRequester",
            get(handler_bounties_by_requester),
        )
        .route("/v1/adapters", get(handler_adapters_for_framework))
        .route(
            "/xrpc/dev.idiolect.query.adaptersForFramework",
            get(handler_adapters_for_framework),
        )
        .route(
            "/v1/adapters/by-invocation-protocol",
            get(handler_adapters_by_invocation_protocol),
        )
        .route(
            "/xrpc/dev.idiolect.query.adaptersByInvocationProtocol",
            get(handler_adapters_by_invocation_protocol),
        )
        .route(
            "/v1/recommendations",
            get(handler_recommendations_starting_from),
        )
        .route(
            "/xrpc/dev.idiolect.query.recommendationsStartingFrom",
            get(handler_recommendations_starting_from),
        )
        .route("/v1/verifications", get(handler_verifications_for_lens))
        .route(
            "/xrpc/dev.idiolect.query.verificationsForLens",
            get(handler_verifications_for_lens),
        )
        .route(
            "/v1/verifications/by-kind",
            get(handler_verifications_by_kind),
        )
        .route(
            "/xrpc/dev.idiolect.query.verificationsByKind",
            get(handler_verifications_by_kind),
        )
        .route("/v1/communities", get(handler_communities_for_member))
        .route(
            "/xrpc/dev.idiolect.query.communitiesForMember",
            get(handler_communities_for_member),
        )
        .route("/v1/communities/by-name", get(handler_communities_by_name))
        .route(
            "/xrpc/dev.idiolect.query.communitiesByName",
            get(handler_communities_by_name),
        )
        .route(
            "/v1/dialects/for-community",
            get(handler_dialects_for_community),
        )
        .route(
            "/xrpc/dev.idiolect.query.dialectsForCommunity",
            get(handler_dialects_for_community),
        )
        .route(
            "/v1/adapters/with-verification",
            get(handler_adapters_with_verification),
        )
        .route(
            "/xrpc/dev.idiolect.query.adaptersWithVerification",
            get(handler_adapters_with_verification),
        )
        .route("/v1/beliefs/about", get(handler_beliefs_about_record))
        .route(
            "/xrpc/dev.idiolect.query.beliefsAboutRecord",
            get(handler_beliefs_about_record),
        )
        .route("/v1/beliefs/by-holder", get(handler_beliefs_by_holder))
        .route(
            "/xrpc/dev.idiolect.query.beliefsByHolder",
            get(handler_beliefs_by_holder),
        )
        .route(
            "/v1/vocabularies/by-world",
            get(handler_vocabularies_with_world),
        )
        .route(
            "/xrpc/dev.idiolect.query.vocabulariesWithWorld",
            get(handler_vocabularies_with_world),
        )
        .route(
            "/v1/vocabularies/by-name",
            get(handler_vocabularies_by_name),
        )
        .route(
            "/xrpc/dev.idiolect.query.vocabulariesByName",
            get(handler_vocabularies_by_name),
        )
}
