// @generated by idiolect-codegen. do not edit.
// source: dev.idiolect.communityRelease

// A signed, immutable release of community-governed definitions and approved changes. The record is an index over content-addressed artifacts; a portable release bundle carries the full manifest and evidence.

export interface CommunityRelease {
  artifacts?: Artifact[];
  /**
  * Approved dev.idiolect.changeProposal records.
  */
  changes: string[];
  community: string;
  createdAt: string;
  dependencies?: Dependency[];
  /**
  * Digest of canonical unsigned bundle bytes.
  */
  digest: string;
  manifestDigest: string;
  /**
  * Detached signatures over digest.
  */
  signatures: Signature[];
  /**
  * Community release version.
  */
  version: string;
}

export interface Artifact {
  digest: string;
  location: string;
  mediaType?: string;
}

export interface Dependency {
  /**
  * Peer community record or DID.
  */
  community: string;
  relation: DependencyRelation;
  release?: string;
  updatePolicy: DependencyUpdatePolicy;
}

export interface Signature {
  algorithm: SignatureAlgorithm;
  publicKey: string;
  signature: string;
  signedAt: string;
  signer: string;
}

export type DependencyRelation = "follows" | "extends" | "bridges" | "forked-from" | string & {};

export type DependencyUpdatePolicy = "review" | "follow-compatible" | "pinned" | string & {};

export type SignatureAlgorithm = "ES256" | string & {};
