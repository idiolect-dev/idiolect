{
  "defs": {
    "main": {
      "description": "Every migration run attached to the selected governed change.",
      "output": {
        "description": "Paged<EnvelopedEntry<R>> — identical shape to the REST endpoint for this query.",
        "encoding": "application/json"
      },
      "parameters": {
        "properties": {
          "change_uri": {
            "type": "string"
          },
          "cursor": {
            "description": "Opaque pagination cursor returned by a prior page of results.",
            "type": "string"
          },
          "limit": {
            "description": "Maximum number of entries to return in one page.",
            "maximum": 200,
            "minimum": 1,
            "type": "integer"
          }
        },
        "required": [
          "change_uri"
        ],
        "type": "params"
      },
      "type": "query"
    }
  },
  "description": "XRPC facade for the `migration_runs_for_change` catalog query. Mirrors the REST endpoint at `/v1/migrations/for-change`; both paths invoke the same handler.",
  "id": "dev.idiolect.query.migrationRunsForChange",
  "lexicon": 1
}
