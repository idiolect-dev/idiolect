{
  "defs": {
    "main": {
      "description": "Every governed change owned by the selected community.",
      "output": {
        "description": "Paged<EnvelopedEntry<R>> — identical shape to the REST endpoint for this query.",
        "encoding": "application/json"
      },
      "parameters": {
        "properties": {
          "community_uri": {
            "type": "string"
          },
          "cursor": {
            "description": "Opaque pagination cursor returned by a prior page of results.",
            "type": "string"
          },
          "limit": {
            "description": "Maximum number of entries to return in one page.",
            "maximum": 200,
            "minimum": 1,
            "type": "integer"
          }
        },
        "required": [
          "community_uri"
        ],
        "type": "params"
      },
      "type": "query"
    }
  },
  "description": "XRPC facade for the `change_proposals_for_community` catalog query. Mirrors the REST endpoint at `/v1/changes/for-community`; both paths invoke the same handler.",
  "id": "dev.idiolect.query.changeProposalsForCommunity",
  "lexicon": 1
}
