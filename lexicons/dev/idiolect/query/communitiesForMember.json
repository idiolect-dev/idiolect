{
  "defs": {
    "main": {
      "description": "Every community whose `members` list includes the given DID.",
      "output": {
        "description": "Paged<EnvelopedEntry<R>> — identical shape to the REST endpoint for this query.",
        "encoding": "application/json"
      },
      "parameters": {
        "properties": {
          "cursor": {
            "description": "Opaque pagination cursor returned by a prior page of results.",
            "type": "string"
          },
          "limit": {
            "description": "Maximum number of entries to return in one page.",
            "maximum": 200,
            "minimum": 1,
            "type": "integer"
          },
          "member_did": {
            "type": "string"
          }
        },
        "required": [
          "member_did"
        ],
        "type": "params"
      },
      "type": "query"
    }
  },
  "description": "XRPC facade for the `communities_for_member` catalog query. Mirrors the REST endpoint at `/v1/communities`; both paths invoke the same handler.",
  "id": "dev.idiolect.query.communitiesForMember",
  "lexicon": 1
}
