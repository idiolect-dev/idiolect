{
  "defs": {
    "main": {
      "description": "Every community whose name matches (case-insensitive).",
      "output": {
        "description": "Paged<EnvelopedEntry<R>> — identical shape to the REST endpoint for this query.",
        "encoding": "application/json"
      },
      "parameters": {
        "properties": {
          "cursor": {
            "description": "Opaque pagination cursor returned by a prior page of results.",
            "type": "string"
          },
          "limit": {
            "description": "Maximum number of entries to return in one page.",
            "maximum": 200,
            "minimum": 1,
            "type": "integer"
          },
          "name": {
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "type": "params"
      },
      "type": "query"
    }
  },
  "description": "XRPC facade for the `communities_by_name` catalog query. Mirrors the REST endpoint at `/v1/communities/by-name`; both paths invoke the same handler.",
  "id": "dev.idiolect.query.communitiesByName",
  "lexicon": 1
}
