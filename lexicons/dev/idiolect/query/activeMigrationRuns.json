{
  "defs": {
    "main": {
      "description": "Every planned, running, or paused migration run.",
      "output": {
        "description": "Paged<EnvelopedEntry<R>> — identical shape to the REST endpoint for this query.",
        "encoding": "application/json"
      },
      "parameters": {
        "properties": {
          "cursor": {
            "description": "Opaque pagination cursor returned by a prior page of results.",
            "type": "string"
          },
          "limit": {
            "description": "Maximum number of entries to return in one page.",
            "maximum": 200,
            "minimum": 1,
            "type": "integer"
          }
        },
        "type": "params"
      },
      "type": "query"
    }
  },
  "description": "XRPC facade for the `active_migration_runs` catalog query. Mirrors the REST endpoint at `/v1/migrations/active`; both paths invoke the same handler.",
  "id": "dev.idiolect.query.activeMigrationRuns",
  "lexicon": 1
}
