{
  "defs": {
    "main": {
      "description": "Every adapter declared for the given framework (case-insensitive).",
      "output": {
        "description": "Paged<EnvelopedEntry<R>> — identical shape to the REST endpoint for this query.",
        "encoding": "application/json"
      },
      "parameters": {
        "properties": {
          "cursor": {
            "description": "Opaque pagination cursor returned by a prior page of results.",
            "type": "string"
          },
          "framework": {
            "type": "string"
          },
          "limit": {
            "description": "Maximum number of entries to return in one page.",
            "maximum": 200,
            "minimum": 1,
            "type": "integer"
          }
        },
        "required": [
          "framework"
        ],
        "type": "params"
      },
      "type": "query"
    }
  },
  "description": "XRPC facade for the `adapters_for_framework` catalog query. Mirrors the REST endpoint at `/v1/adapters`; both paths invoke the same handler.",
  "id": "dev.idiolect.query.adaptersForFramework",
  "lexicon": 1
}
