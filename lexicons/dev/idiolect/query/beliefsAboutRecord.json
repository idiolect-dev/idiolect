{
  "defs": {
    "main": {
      "description": "Every belief record whose subject's at-uri matches.",
      "output": {
        "description": "Paged<EnvelopedEntry<R>> — identical shape to the REST endpoint for this query.",
        "encoding": "application/json"
      },
      "parameters": {
        "properties": {
          "cursor": {
            "description": "Opaque pagination cursor returned by a prior page of results.",
            "type": "string"
          },
          "limit": {
            "description": "Maximum number of entries to return in one page.",
            "maximum": 200,
            "minimum": 1,
            "type": "integer"
          },
          "subject_uri": {
            "type": "string"
          }
        },
        "required": [
          "subject_uri"
        ],
        "type": "params"
      },
      "type": "query"
    }
  },
  "description": "XRPC facade for the `beliefs_about_record` catalog query. Mirrors the REST endpoint at `/v1/beliefs/about`; both paths invoke the same handler.",
  "id": "dev.idiolect.query.beliefsAboutRecord",
  "lexicon": 1
}
