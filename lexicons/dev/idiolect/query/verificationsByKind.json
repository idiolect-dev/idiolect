{
  "defs": {
    "main": {
      "description": "Every verification record whose `kind` matches.",
      "output": {
        "description": "Paged<EnvelopedEntry<R>> — identical shape to the REST endpoint for this query.",
        "encoding": "application/json"
      },
      "parameters": {
        "properties": {
          "cursor": {
            "description": "Opaque pagination cursor returned by a prior page of results.",
            "type": "string"
          },
          "kind": {
            "description": "one of `roundtrip-test`, `property-test`, `formal-proof`, `conformance-test`, `static-check`, `convergence-preserving`.",
            "type": "string"
          },
          "limit": {
            "description": "Maximum number of entries to return in one page.",
            "maximum": 200,
            "minimum": 1,
            "type": "integer"
          }
        },
        "required": [
          "kind"
        ],
        "type": "params"
      },
      "type": "query"
    }
  },
  "description": "XRPC facade for the `verifications_by_kind` catalog query. Mirrors the REST endpoint at `/v1/verifications/by-kind`; both paths invoke the same handler.",
  "id": "dev.idiolect.query.verificationsByKind",
  "lexicon": 1
}
